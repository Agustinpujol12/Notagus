<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:minHeight="28dp"
    android:paddingTop="2dp"
    android:paddingBottom="2dp">

    <!-- Puntito a la izquierda (negro y centrado) -->
    <ImageView
        android:id="@+id/dot"
        android:layout_width="6dp"
        android:layout_height="6dp"
        android:layout_alignParentStart="true"
        android:layout_centerVertical="true"
        android:layout_marginEnd="8dp"
        android:src="@drawable/bg_task_dot" />

    <!-- Título (2 líneas, ellipsis) -->
    <TextView
        android:id="@+id/tvTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_toEndOf="@id/dot"
        android:layout_toStartOf="@id/ivPinned"
        android:text="Título de tarea"
        android:textColor="@android:color/black"
        android:textSize="16sp"
        android:gravity="center_vertical"
        android:maxLines="2"
        android:ellipsize="end" />

    <!-- (opcional) pin a la derecha -->
    <ImageView
        android:id="@+id/ivPinned"
        android:layout_width="18dp"
        android:layout_height="18dp"
        android:layout_alignParentEnd="true"
        android:layout_centerVertical="true"
        android:src="@drawable/ic_pin"
        android:visibility="gone"/>
</RelativeLayout>
